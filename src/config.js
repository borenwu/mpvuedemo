//配置项

const host = 'https://pdki2fed.qcloud.la';
// const host = 'http://localhost:5757'

const config = {
    host: host,
    loginUrl: `${host}/weapp/login`,
    userUrl: `${host}/weapp/user`
}

export default config
